<h2>Add a new comment of comment</h2>

<%= simple_form_for([@comment_to_find, @comment]) do |f| %>
  <div class="field">
    <div class="control">
      <%= f.input :text %>
      <%= f.button :submit, 'Create a new comment of comment' %>
    </div>
  </div>
<% end %>

errors: <%= @comment.errors.messages %>
</div>